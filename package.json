{
  "name": "eric-orchestra",
  "version": "1.0.0",
  "scripts": {
    "dev": "parcel index.html",
    "build": "parcel build index.html",
    "deploy": "rm -rf dist && npm run build && echo 'eric-orchestras.euans.space' > dist/CNAME && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "parcel": "^2.8.3"
  }
}
